@model AIRMAM5.DBEntity.Models.Shared.LoginIdDateSerarchModel

@*@using (Html.BeginForm("Search", "L_Log", FormMethod.Post, new { role = "form", @class = "ui inverted form", @id = "SearchForm" }))
    {*@
<form class="ui inverted form" role="form" id="SearchForm">
    <div class="six fields">
        <div class="three wide field">
            <label>@Html.LabelFor(model => model.UserId) </label>
            <input type="hidden" name="SelfAccount" value="@Model.UserId" id="SelfAccount" />
            @Html.DropDownListFor(model => model.UserId, Model.LoginIdList, new { @class = "ui search selection dropdown" })
        </div>
        <div class="two wide ui calendar required field">
            @*<label>@Html.LabelFor(model => model.BeginDate)</label>*@
            <label>操作日期(起)</label>
            @Html.TextBoxFor(model => model.BeginDate, new { type = "text", placeholder = "起", @id = "sdate" })
        </div>
        <div class="two  wide ui calendar required field">
            @*<label>@Html.LabelFor(model => model.EndDate)</label>*@
            <label>操作日期(迄)</label>
            @Html.TextBoxFor(model => model.EndDate, new { type = "text", placeholder = "迄", @id = "edate" })
        </div>
        <div class="four wide field">
            <label style="visibility:hidden;">查詢</label>
            <button type="submit" class="ui yellow button">查詢</button>
            <button type="button" name="reset" class="ui button">清空</button>
            @*<button type="reset" class="ui button">清空</button>*@
        </div>
    </div>
</form>
@*}*@
