@model AIRMAM5.DBEntity.Models.Subject.SubjectViewModel
@{ 
    var _codeSer = new AIRMAM5.DBEntity.Services.CodeService();
}

@HtmlHelper.HtmlModal("DetailModal", "檢視主題資料",
@<from class="ui inverted form" name="Detail">
    <dvi class="two fields">
        <div class="field">
            @Html.LabelFor(model => model.SubjectId)
            @Html.TextBoxFor(model => model.SubjectId, new { @type = "text", @readonly = "readonly" })
        </div>
        <div class="required field">
            @Html.LabelFor(model => model.Title)
            @Html.TextBoxFor(model => model.Title, new { @type = "text", @readonly = "readonly" })
        </div>
    </dvi>
    <div class="field">
        @Html.LabelFor(m => m.Description)
        @Html.TextAreaFor(m => m.Description, new { @type = "text", @readonly = "readonly" })
    </div>

    @foreach (var itm in Model.SubjectAttributes)
    {
        <div class="field">
            @Html.DisplayTextFor(m => itm.FieldName)
            @*@if (itm.FieldType == "CODE")
            {
                var _clist = _codeSer.GetCodeItemList(itm.FieldCodeId, null, false);
                @Html.DropDownList(itm.Field, _clist, new { @class = "ui multiple selection fluid dropdown disabled", @multiple = "", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBoxFor(m => itm.FieldValue, new { @type = "text", @readonly = "readonly" })
            }*@
            @Html.TextBoxFor(m => itm.FieldValue, new { @type = "text", @readonly = "readonly" })
        </div>
    }

</from>,
 @<div>
     <button class="ui cancel  button" type="button">關閉</button>
 </div>
)

