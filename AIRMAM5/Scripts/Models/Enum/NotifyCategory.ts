/**訊息通知類別 */
export enum NotifyCategory {
    /**預設(全部帳號) */
    Default = 0,
    /**角色群組 */
    Groups = 1,
    /** 指定帳號 */
    Assign = 2,
}

/**訊息通知中文類別 */
export enum NotifyCategoryChinese {
    /**預設(全部帳號) */
    預設,
    /**角色群組 */
    角色群組,
    /** 指定帳號 */
    指定帳號,
}
